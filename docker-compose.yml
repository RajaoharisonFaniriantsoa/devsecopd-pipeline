version: '3.8'
services:
  app:
    image: ${IMAGE}
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - ENV=production
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
